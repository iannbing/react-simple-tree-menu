/** @type {import('next').NextConfig} */
const nextConfig = {
  // The library ships an ESM build and a CSS subpath export — Next 15
  // resolves both natively. No transpilePackages needed.
  reactStrictMode: true,
};

export default nextConfig;
